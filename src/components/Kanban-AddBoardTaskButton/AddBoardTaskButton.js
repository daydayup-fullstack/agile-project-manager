import React from "react";
import "./AddBoardTaskButton.css";

const AddBoardTaskButton = () => {
    return (
        <div className={"addBoardTaskButton"} onClick={() => {}}>
            <span className={"material-icons"}>add</span>
        </div>
    );
};

export default AddBoardTaskButton;
